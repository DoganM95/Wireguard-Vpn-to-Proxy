http_port 0.0.0.0:3128

# Allow local LAN access only
acl allowed_clients src 10.0.0.0/8
http_access allow allowed_clients

# Deny everyone else
http_access deny all

# No caching
cache deny all

# Minimal logging
access_log stdio:/dev/null
